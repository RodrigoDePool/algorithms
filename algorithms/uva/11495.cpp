#include <iostream>
#include <vector>
using namespace std; 

int bsort(vector<int> a, int min, int max, acumm){


}


int main(){
    int first = true;
    while(true){
        int n;
        cin >> n;
        if(n==0) break;
        if(first)
            first = false;
        else
            cout << endl;

        vector<int> seq;
        for(int ii=0; i<n;i++){
            int aux;
            cin>>aux;
            seq.push_back(aux-1);
        
        }
        int res = bsort(aux,-1,aux.size(),0);
    }

}
